const router = require('express').Router();
const {getAllCategory,findCategory} = require('../controllers/benefitCategory')

// router
// .get('/',getAllCategory)
// // .post('/new', addNewCategory)

// router
// .get('/edit/:id',findCategory)
// // .patch('/edit/:id',editCategory)

module.exports = router;
